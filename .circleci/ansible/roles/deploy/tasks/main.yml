- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Copy index test page
  template:
    src: "backend/dist/"
    dest: "~/web/"
    directory_mode:

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f