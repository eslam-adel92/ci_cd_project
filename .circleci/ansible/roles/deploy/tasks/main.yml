- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Copy index test page
  template:
    src: "backend/dist/"
    dest: "~/web/"
    remote_src: yes

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f