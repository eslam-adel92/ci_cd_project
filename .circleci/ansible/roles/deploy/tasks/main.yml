- name: Creates directory
  file:
    path: ~/web
    state: directory

- name: Copy dist content to s3 bucket.
  template:
    src: "../../backend/dist/"
    dest: "~/web/"

- name: Executing node
  shell: |
    pm2 start ~/web/main.js -f